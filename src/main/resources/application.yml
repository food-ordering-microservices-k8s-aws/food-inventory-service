server:
  port: 9093

spring:
  application:
    name: food-inventory-service-service-service
  datasource:
    url: jdbc:mysql://localhost:3308/foodinventorydb
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8717/eureka/